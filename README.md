New VATUSA Front end written in React

Uses NodeJS 8 available [here](https://nodejs.org/en/download/current/).

To fix `$ npm run start` issue on MacOS Sierra (10.12.x) use `$ brew reinstall watchman`
